services:
  - type: web
    name: ai-editor-hocuspocus
    env: node
    buildCommand: npm install
    startCommand: npm run start:hocuspocus
    envVars:
      - key: NODE_ENV
        value: production
